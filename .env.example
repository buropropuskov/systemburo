# === База данных ===
DB_NAME=auto_registry
DB_USER=postgres
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DATABASE_URL=postgres://postgres:CHANGE_ME@localhost/auto_registry

# === JWT (сгенерировать: openssl rand -hex 32) ===
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL
JWT_REFRESH_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL

# === Backend ===
BIND_HOST=127.0.0.1
BIND_PORT=8080
RUST_LOG=info
