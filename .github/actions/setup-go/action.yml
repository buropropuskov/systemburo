name: Setup Go
description: Setup Go toolchain with module cache and download dependencies.
runs:
  using: composite
  steps:
    - uses: actions/setup-go@v5
      with:
        go-version-file: go.mod
        cache: true
    - name: Download modules
      shell: bash
      run: go mod download
