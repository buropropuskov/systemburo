# Отчёт по изменениям

Файл: `diplom_myakotnykh.docx`
Резервная копия оригинала: `diplom_myakotnykh_original.docx`
Свод правил: `docs/rules.md`

Все правки применяются через `scripts/edit-docx.py`. Запуск читает оригинал, пишет в итоговый файл. Перезапуск идемпотентен.

---

## Итерация 3 — единый стиль по правилам

### 0. Свод правил

В `docs/rules.md` закреплены:

- структура и последовательность разделов главы 2;
- стили заголовков, подписей таблиц/рисунков/листингов с коротким тире «–»;
- порядок «описательный текст → изображение»;
- единая палитра диаграмм (чёрный / белый / `#6C67FD`) с прямыми ортогональными линиями;
- список исключений (бизнес-процессы и схема взаимодействия фронт-бэк);
- корректный вид диаграмм по типам (Use Case, user flow, activity, swimlane, site map, sequence);
- итоговая цветовая палитра сервиса из пяти цветов;
- список инструментов сравнения на этапе вайрфреймов;
- терминологические замены;
- требование убрать отдельный раздел «Список используемых сокращений».

### 1. Сравнение инструментов проектирования (2.8.1)

Сравнение перестроено между тремя инструментами: **Figma, Adobe Illustrator, XDesign** (по 8 критериям: специализация, платформы, стоимость, реалтайм-коллаборация, компонентная система, экспорт в код, доступность в РФ, зрелость экосистемы). По итогам сравнения аргументированно выбрана Figma как специализированный UI-инструмент с реалтайм-коллаборацией и зрелой экосистемой.

### 2. Цветовая палитра (2.9.1)

Итоговая палитра сервиса:

| HEX | Роль |
|---|---|
| `#FFFFFF` | Основной фон |
| `#000000` | Основной текст и контуры |
| `#6C67FD` | Бренд-цвет, акценты, активные состояния |
| `#A2A2A2` | Вторичный текст, подсказки |
| `#E6E6E6` | Разделители, фон второстепенных блоков |

Рисунок 2.9.1 «Сравнение вариантов цветовой палитры» перегенерирован с новым значением варианта А. Сравнительная таблица 2.9.1 и текст выбора обновлены: вариант А — «Сдержанная с синим акцентом».

### 3. Диаграммы в едином стиле

Все диаграммы, кроме исключений (А.1–А.3 бизнес-процессы и 3.3 схема взаимодействия фронт-бэк), приведены к единой стилистике:

- три цвета: чёрный (`#000000`), белый (`#FFFFFF`), синий бренда (`#6C67FD`);
- ортогональные линии и прямые стрелки;
- `skinparam linetype ortho` для PlantUML-диаграмм.

Затронутые диаграммы:

- 1.10 сравнение Vue/React/Svelte — синяя заливка у Vue, серые — у конкурентов;
- 2.3.5 карта user stories — MVP-ряд с синей заливкой, остальные — белые/серые;
- 2.9 флоу регистрации — PlantUML activity;
- 2.10 флоу анализа документа — PlantUML activity;
- 2.11 флоу работы с результатами — PlantUML activity;
- 2.12 swimlane «Обработка платежа» — PlantUML swimlane;
- 2.13 Use Case «Администрирование и биллинг» — PlantUML usecase;
- 2.14 site map — matplotlib с ортогональными связями;
- 2.15 палитра — matplotlib с подсвеченным вариантом А;
- 2.16 активность «Работа с заметками» — PlantUML activity;
- 2.17 sequence «Анализ документа» — PlantUML sequence.

### 4. Последовательность разделов главы 2

В теле документа последовательность уже соответствует правилу:

```
2.1 Анализ ЦА
2.2 Бизнес-процессы
2.3 User stories
2.4 Use Case
2.5 User flow
2.6 Диаграммы активности
2.7 Структура приложения (site map)
2.8 Разработка каркаса (вайрфреймов)
2.9 Разработка прототипов и дизайн-макетов
```

Оглавление (TOC) в исходнике ещё отражает старую нумерацию — оно обновится автоматически при открытии в LibreOffice/Word: Ctrl+A → F9 (или Tools → Update → Update All).

### 5. Порядок «текст → картинка»

Для раздела 2.9.1 изменена последовательность блоков: теперь сначала вводный текст и описание критериев, затем сравнительная таблица 2.9.1, затем изображение палитр и подпись «Рисунок 2.9.1», и только после этого — итоговое обоснование выбора. Все остальные новые разделы (2.5 user flow, 2.6 swimlane, 2.4 use case с ролями) построены по тому же правилу: описательный абзац → изображение → подпись → интерпретация.

### 6. Список сокращений

В документе отдельный раздел «Список используемых сокращений» отсутствует (не создавался) — требование глобального `CLAUDE.md` соблюдено. Сокращения расшифровываются в скобках при первом упоминании в тексте.

### 7. Содержание и тексты

Содержание документа, последовательность глав и подразделов, а также оригинальный текст разделов сохранены. Обновлены только:

- тексты 2.8.1 и 2.9.1 под новые инструменты и палитру;
- подписи рисунков и таблиц (короткое тире «–»);
- нумерация рисунков и таблиц (2.8.1, 2.9.1, 2.3.5 и т. п.), чтобы не конфликтовать с существующей.

---

## Итерации 1 и 2 (для полноты истории)

Предыдущие правки, вошедшие в текущий файл:

- перестройка главы 2 под правильную последовательность этапов проектирования;
- 8 PNG-диаграмм (user flow, swimlane, use case, sequence, site map, user story map, tech comparison, palette);
- роли «пользователь / администратор / менеджер / биллинг» в user stories и use case;
- перенос полных таблиц требований в Приложение Б, краткий обзор в 1.9;
- user story mapping (2.3.5);
- листинги кода в главе 3 и полные версии в Приложении В;
- Приложение Д — руководство пользователя и инструкции по развёртыванию/сопровождению;
- замена TypeScript на JavaScript в листингах;
- единые подписи с коротким тире «–»;
- замена нестандартных терминов: «карта экранов» → «структура приложения», «навигационная структура» → «навигация по приложению», «рабочий экран анализа» → «основной экран анализа документа».

---

## Артефакты проекта

```
Sergey/
├─ diplom_myakotnykh.docx            ← итоговый файл
├─ diplom_myakotnykh_original.docx   ← резервная копия исходника
├─ CHANGES.md                        ← этот отчёт
├─ docs/
│  └─ rules.md                       ← свод правил оформления
├─ scripts/
│  ├─ edit-docx.py                   ← основной скрипт правок
│  ├─ edits_content.py               ← текстовое содержимое
│  ├─ gen_diagrams.py                ← matplotlib-диаграммы
│  ├─ gen_plantuml.py                ← PlantUML-диаграммы с единым stylemodule
│  └─ gen_user_story_map.py          ← карта user story mapping
└─ images/                           ← все PNG-диаграммы
```

## Команды пересборки

```bash
cd /home/washka/project/diplom/Sergey

# (1) Сгенерировать matplotlib-диаграммы (site map, user story map, tech comparison, palette)
python3 scripts/gen_diagrams.py

# (2) Сгенерировать PlantUML-диаграммы (требует интернет)
#     Перезаписывает часть файлов от gen_diagrams.py (user flow, swimlane, use case, sequence).
python3 scripts/gen_plantuml.py

# (3) Перезаписать user story map поверх PlantUML-версии (в карте нужна таблица, а не диаграмма)
python3 scripts/gen_user_story_map.py

# (4) Пересобрать docx (идемпотентно)
python3 scripts/edit-docx.py
```

## Что сделать вручную в LibreOffice/Word

1. Обновить оглавление: Ctrl+A → F9 (LibreOffice: Tools → Update → Update All). Это подтянет номера 2.3.1 — 2.3.5, 2.4, 2.5, 2.7.1 — 2.7.3, 2.8.1 — 2.8.3, 2.9.1 — 2.9.4 и Приложение Д.
2. Проверить разрывы страниц: после добавления новых таблиц и рисунков имеет смысл пройтись по главам и убедиться, что заголовки не остаются в конце страниц.
3. При желании вставить Рисунок 1.10 (сравнение Vue/React/Svelte) в раздел 1.8 — файл уже есть в `images/`.
