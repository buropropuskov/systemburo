import { defineAsyncComponent } from 'vue';

/**
 * Редактор текста, загружаемый по требованию.
 *
 * TextConstructor тянет за собой tiptap и весь prosemirror - около 140 КБ, нужных только
 * на экранах с текстом: новости, объявления и руководство, инструкции к таблицам, согласие,
 * сообщение заявки. Статический импорт держал их в стартовом чанке у каждого пользователя,
 * поэтому шесть потребителей берут редактор отсюда (#2521).
 */
export default defineAsyncComponent(() => import('../TextConstructor.vue'));
